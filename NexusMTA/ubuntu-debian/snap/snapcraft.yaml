name: nexus-mta-ubuntu
version: '1.0.0'
summary: Nexus Mail Transfer Agent (Ubuntu/Debian)
description: |
  Mail Transfer Agent (MTA) for Nexus mail system.
  Native C implementation with Postfix-compatible features for Ubuntu/Debian Linux.
base: core22
confinement: strict
grade: stable
architectures:
  - build-on: amd64
    run-on: amd64

apps:
  nexus-mta:
    command: bin/nexus-mta
    daemon: simple
    plugs:
      - network
      - network-bind

parts:
  nexus-mta:
    plugin: make
    source: .
    source-type: local
