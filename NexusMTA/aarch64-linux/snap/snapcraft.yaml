name: nexus-mta-aarch64
version: '1.0.0'
summary: Nexus Mail Transfer Agent (ARM64)
description: |
  Mail Transfer Agent (MTA) for Nexus mail system.
  ARM64 Linux version.
base: core22
confinement: strict
grade: stable
architectures:
  - build-on: arm64
    run-on: arm64

apps:
  nexus-mta:
    command: bin/nexus-mta
    daemon: simple
    plugs:
      - network
      - network-bind

parts:
  nexus-mta:
    plugin: make
    source: .
    source-type: local
